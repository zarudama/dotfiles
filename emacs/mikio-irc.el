;;-----------------------------------------------------------------
;; irc
;; rcircは標準添付
;;-----------------------------------------------------------------
(setq rcirc-server-alist
      '(("irc.freenode.net" :channels ("#emacs-lisp-ja" "#emacs-ja" "#lisp-ja"))
        ("irc.media.kyoto-u.ac.jp" :channels ("#ruby"))))
(setq rcirc-log-flag t)

